code ทั้งสองอันนี้จะแสดง alert ออกมาเหมือนกันทั้งหมดหรือไม่

let i = 0;
while (++i < 5) alert( i );

let i = 0;
while (i++ < 5) alert( i );

Answer = ไม่เหมือน เงื่อนไขแรกจะแสดง ค่า 5
                 เงื่อนไขที่สองจะแสดง ค่า 6